# Google Cloud Storage Setup

1. Create a GCS bucket.
2. Grant service account write access.
3. Set `CLOUD_STORAGE_BUCKET` in backend `.env`.
4. Ensure Google credentials are available via environment (`GOOGLE_APPLICATION_CREDENTIALS`).
